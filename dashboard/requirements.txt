# Minimal requirements for Streamlit Community Cloud. lightgbm/psycopg2-style
# native-dependency lessons learned from the MarketPulse deployment: pin
# past known-broken combinations, and see packages.txt for the matching
# apt-level dependency (libgomp1) lightgbm needs at runtime.
streamlit==1.59.2
plotly==5.22.0
pandas==2.2.2
pyarrow==16.1.0
numpy==1.26.4
lightgbm==4.5.0
boto3==1.34.144
