# Full local dev environment: data prep, model training, dashboard, and
# tests. dashboard/requirements.txt is a separate, smaller subset for
# Streamlit Cloud deployment (see that file's own comment for why).

# --- Data prep (one-time, local only - Lambda never touches Hugging Face) ---
datasets==2.20.0
huggingface_hub==0.24.0

# --- Model training ---
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
lightgbm==4.5.0
scikit-learn==1.5.1
scipy==1.13.1
joblib==1.4.2

# --- Dashboard ---
streamlit==1.59.2
plotly==5.22.0
boto3==1.34.144

# --- Testing / quality ---
pytest==8.2.2
ruff==0.5.0
